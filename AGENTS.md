# AGENTS.md

## Cursor Cloud specific instructions

This is a **GitHub Profile README repository** (`felixgever/felixgever`). It contains only a single `README.md` file that GitHub renders on the owner's profile page.

### Key facts

- **No application, services, or build system** — the entire repo is one Markdown file.
- **No dependencies** to install (no `package.json`, `requirements.txt`, etc.).
- **No lint, test, or build commands** — edits are validated visually on GitHub.
- **No dev server** to start; changes are previewed by viewing the rendered Markdown.

### Development workflow

1. Edit `README.md`.
2. Commit and push to `main`.
3. View the rendered result on the GitHub profile page.
